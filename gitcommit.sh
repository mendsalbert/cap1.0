#!/bin/bash

# Add all changes to git
git add .

# Commit changes with a generic message
# You can modify the commit message as needed
git commit -m "cap"

# Push changes to the current branchd
git push origin main
